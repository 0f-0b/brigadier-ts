{
  "fmt": {
    "files": {
      "exclude": [
        "npm"
      ]
    }
  },
  "lint": {
    "files": {
      "exclude": [
        "npm"
      ]
    }
  },
  "test": {
    "files": {
      "exclude": [
        "npm"
      ]
    }
  },
  "tasks": {
    "lint": "deno fmt --check && deno lint && deno check --lock --remote mod.ts scripts/build_npm.ts",
    "test": "deno test --lock --check=all",
    "bench": "deno bench --unstable --check=all"
  }
}
